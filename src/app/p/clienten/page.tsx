import { Placeholder } from "@/components/placeholder";

export default function Page() {
  return <Placeholder eyebrow="Actief, gepauzeerd, afgerond" title="Cliënten" note="Alle cliënten op een rij" step={6} />;
}
